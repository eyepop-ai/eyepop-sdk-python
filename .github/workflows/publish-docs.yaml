name: Publish docs

# docs/gitbook is the customer documentation for this repository. The docs site
# is one GitBook space, so the pages are mirrored into eyepop-docs-external
# under sdks/python rather than published as a separate space.

on:
  push:
    branches: [main]
    paths:
      - 'docs/gitbook/**'
      - '.github/workflows/publish-docs.yaml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: publish-docs
  cancel-in-progress: false

jobs:
  publish:
    name: Mirror docs to the docs site
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: false

      # This repository is public and eyepop-actions is private, so the action
      # is checked out and used from a local path rather than by owner/repo ref.
      - uses: actions/checkout@v7
        with:
          repository: eyepop-ai/eyepop-actions
          ref: v1
          token: ${{ secrets.GH_TOKEN }}
          path: .github/eyepop-actions

      - uses: ./.github/eyepop-actions/publish-docs
        with:
          sources: |
            docs/gitbook/:sdks/python
          token: ${{ secrets.GH_TOKEN }}
